import DS from 'ember-data';

export default DS.Store.extend({
  // push: function () {
  //   console.log('Using custom push!');
  //   return this._super.apply(this, arguments);
  // },

  // createRecord: function () {
  //   console.log('Using custom createRecord!');
  //   return this._super.apply(this, arguments);
  // }

});
